spring:
  profiles: local
  config:
    activate:
      on-profile: local
  datasource:
    url: 'jdbc:mysql://localhost:3306/photos?serverTimezone=UTC'
    username: root
    password: rootroot
    driver-class-name: 'com.mysql.cj.jdbc.Driver'
  jpa:
    show-sql: true
name: local
enviroment: local
enable: true

---
spring:
  profiles: docker
  datasource:
    url: 'jdbc:mysql://spring-boot-demo-db-1:3306/photos?serverTimezone=UTC'
    username: root
    password: root
    driver-class-name: 'com.mysql.cj.jdbc.Driver'
  jpa:
    hibernate.ddl-auto: update
    generate-ddl: false
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
config:
  activate:
    on-profile: docker
name: docker
environment: docker
enabled: false